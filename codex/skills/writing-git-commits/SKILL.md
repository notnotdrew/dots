---
name: writing-git-commits
description: Writes clear Git commit messages from staged or working changes using Tim Pope style. Use when the user asks for a commit message, wants to commit staged changes, or wants to amend a commit message.
---

# Writing Git Commits

Use this skill when the task is to draft or apply a Git commit message from real repository changes.

## Quick Start

Default workflow:

1. Inspect the relevant changes with `git diff --cached` or `git diff`.
2. Check for project-specific commit templates.
3. Draft a focused subject in imperative mood.
4. Add a body only when the change needs context.
5. If the user asked to commit, use a non-interactive `git commit` command.

## Instructions

### 1. Follow the iron law

Never mention Codex, AI, OpenAI, Anthropic, Claude, or tool attribution in the commit message.

Do not add:

- `Co-authored-by` lines for AI tools
- "Generated by AI" notes
- tool names or model names

### 2. Inspect the actual change

Before drafting a message:

- read `git diff --cached` for staged work
- if nothing is staged and the user wants help naming the work, read `git diff`
- use `git status --short` to understand file scope
- if useful, read recent history with `git log --oneline -10`

Write the message from the real diff, not from filenames alone.

### 3. Check for project-specific templates

Before defaulting to Tim Pope style, inspect:

1. `git config commit.template`
2. `.gitmessage` in the repo root
3. `.git/COMMIT_EDITMSG` when amending or when existing template comments matter

If the repo has an established template or convention, follow it. Otherwise use Tim Pope style.

### 4. Use Tim Pope style by default

Subject rules:

- aim for about 50 characters
- never exceed 72 characters
- capitalize the first word
- do not end with a period
- use imperative mood

Completion test:

`If applied, this commit will [subject]`

If the sentence sounds wrong, rewrite the subject.

Common verbs:

- `Add`
- `Fix`
- `Update`
- `Refactor`
- `Remove`
- `Rename`
- `Move`
- `Extract`
- `Simplify`
- `Improve`
- `Implement`
- `Document`

### 5. Decide whether the body is needed

Use subject only when the change is narrow and obvious from the diff.

Add a body when the change:

- spans multiple files
- has non-obvious motivation
- includes tradeoffs or caveats
- introduces breaking behavior
- should reference issues or follow-up work

Body rules:

- leave one blank line after the subject
- wrap at 72 characters
- explain what changed and why
- do not narrate low-level implementation details the diff already shows

### 6. Match the response to the user’s ask

If the user asked for a commit message only:

- provide the proposed subject and body
- briefly explain the theme if it is not obvious

If the user asked to commit:

- verify whether the right changes are staged
- draft the message
- run a non-interactive commit command
- use `--amend` only when the user explicitly asked for it

### 7. Validate before finalizing

Check:

- subject passes the completion test
- subject is imperative
- subject starts capitalized
- subject has no trailing period
- subject stays within length limits
- blank line exists before the body
- body lines wrap at 72 characters
- body explains what and why
- issue references appear at the end when used
- no AI attribution appears anywhere

## Examples

**Simple change**

```text
Fix typo in README
```

**Non-trivial change**

```text
Refactor authentication to use JWT tokens

Replace session-backed authentication with JWT validation so
requests can be served by any app node without sticky sessions.
This removes a deployment constraint and makes auth behavior
consistent across the current runtime environments.
```

**Bug fix**

```text
Fix race condition in order processing

Serialize inventory updates so concurrent orders cannot decrement
stock below zero. The previous check-then-update flow allowed
multiple workers to validate the same inventory at once.

Fixes #1234
```

## Reference Files

For detailed commit formatting guidance, read [references/tim-pope-format.md](references/tim-pope-format.md).
