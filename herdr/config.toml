onboarding = false

[[keys.command]]
key = "prefix+shift+a"
type = "shell"
command = "$HOME/bin/herdr-wait-toggle"
description = "toggle WAIT prefix on workspace"

[[keys.command]]
key = "prefix+f"
type = "plugin_action"
command = "vv.golden-ratio.apply"
description = "golden ratio"

# herdr-annotate — prefix+shift+a stays WAIT; copy-context is prefix+shift+c.
[[keys.command]]
key = "prefix+a"
type = "plugin_action"
command = "annotate.capture"
description = "annotate text"

[[keys.command]]
key = "prefix+shift+c"
type = "plugin_action"
command = "annotate.copy-context"
description = "copy annotations as context"

[[keys.command]]
key = "prefix+m"
type = "plugin_action"
command = "annotate.manage"
description = "manage annotations"

[[keys.command]]
key = "prefix+o"
type = "plugin_action"
command = "annotate.open"
description = "review documents in this folder"

[[keys.command]]
key = "prefix+shift+o"
type = "plugin_action"
command = "annotate.last"
description = "review the agent's last reply"

[ui]
# Prompt for a name when creating a workspace instead of generating one.
prompt_new_workspace_name = true

[ui.toast]
delivery = "system"

[theme]
name = "terminal"
auto_switch = false

# The "terminal" theme maps UI surfaces onto static ANSI slots, so the sidebar's
# highlight rows can land on a color with almost no contrast against the host
# background (herdr issues #1023, #1307, #1731). Pin them to gruvbox tones that
# read against this profile's #32302f background.
# selection_bg = Navigate-mode cursor row; active_row_bg = active space/agent row.
[theme.custom]
active_row_bg = "#3c3836"
selection_bg = "#665c54"
